# 测试报告

测试日期：2026-06-01

## 测试环境

- 工作目录：`/home/user/pulse_capacitor_online_eval`
- Python：系统 `python3`
- Web 服务：`ThreadingHTTPServer`
- 浏览器验证：Chrome DevTools MCP

## 数据完整性

- 已下载真实 CSV：21 个。
- 行数合计：12441 行。
- 字段：`Time`, `C100Hz`, `ESR100Hz`, `C1kHz`, `ESR1kHz`, `C10kHz`, `ESR10kHz`。
- 数据来源：Sandia / OEDI / Data.gov 钽电容 260C、5V 偏置高温老化测试。

## 单元测试

命令：

```bash
python3 -m unittest discover -s tests -v
```

结果：

```text
Ran 7 tests
OK
```

覆盖点：

- GM(1,1) 对指数增长序列的拟合精度。
- 级比覆盖区间计算。
- 移动加权平滑长度和首值保持。
- 真实 CSV `cap1-1.csv` 的双参数评估流程。
- 80 点窗口、向后 20 步的滚动历史验证流程。
- 回测起始行、结束行和滑动间隔可配置。
- 预测时长和验证时长按 `Time` 列采样间隔换算为预测步数。

## API 测试

数据集接口：

```text
GET /api/datasets
```

结果摘要：

- 返回 21 个数据集。
- `cap1-1.csv` 返回 594 行。
- 默认评估列为 `C1kHz` 和 `ESR1kHz`。

评估接口：

```text
POST /api/evaluate
dataset = cap1-1.csv
columns = C1kHz, ESR1kHz
window = 80
horizon = 600
```

结果摘要：

- 综合精度：99.88%。
- `C1kHz` 精度：99.97%，级比检验通过。
- `ESR1kHz` 精度：99.78%，级比检验通过。
- 在 600 步预测范围内未达到默认临界值。
- 工程状态：正常。

验证接口：

```text
POST /api/validate
dataset = cap1-1.csv
columns = C1kHz, ESR1kHz
window = 80
validationHorizon = 20
```

结果摘要：

- 综合 MAPE：1.78%。
- `C1kHz`: 495 次滚动验证，MAPE 0.12%，最后预测值 11.19818，最后真实值 11.20800。
- `ESR1kHz`: 495 次滚动验证，MAPE 3.45%，最后预测值 1.37694，最后真实值 1.37210。
- 验证状态：通过。

## 浏览器测试

验证地址：

```text
http://127.0.0.1:8890
```

通过项：

- 页面能自动加载 21 个真实 CSV。
- 默认参数自动运行评估。
- 默认参数自动运行历史滚动验证。
- 汇总状态、结果表格和趋势图正常渲染。
- 验证 MAPE、MAE、RMSE、最后预测值和最后真实值正常渲染。
- 历史验证图标注横轴“真实对比行号”和纵轴“绝对百分比误差 (%)”。
- 状态预测图标注横轴“样本序号”和纵轴“参数值”。
- “运行评估与验证”按钮可重复触发接口计算。
- Chrome 控制台无报错。
- 桌面截图：`test_ui_desktop.png`。
- 移动截图：`test_ui_mobile.png`。
- 验证功能截图：`test_ui_validation_desktop.png`。

## 已知限制

- Playwright CLI 包装脚本在本机缺少执行权限，已改用 `bash` 调用并完成真实浏览器验证。
- 当前数据不是逐次脉冲放电波形，无法验证电压/电流反峰系数和首次过零时间字段。

## 2026-07-20 多模型最优预测测试

新增候选方法：GM(1,1)、持续值预测、线性趋势回归、阻尼 Holt 趋势和 AR(1) 自回归。所有方法使用相同的建模窗口、向后步数和滚动范围，以 MAPE 最低者作为每个参数的最佳模型。

自动化测试：

```text
python3 -m unittest discover -s tests -v
Ran 12 tests
OK
```

严格实验特征数据测试条件：

```text
dataset = ceshishuju_patent_features.csv
window = 80
prediction horizon = 300
validation horizon = 20
validation range = 1-606
stride = 1
```

测试结果：

| 参数 | 最佳方法 | 最佳 MAPE | GM MAPE |
| --- | --- | ---: | ---: |
| VoltageMaxRaw | 持续值预测 | 0.1008% | 0.1776% |
| VoltageFirstZeroTimeUs | GM(1,1) | 0.6406% | 0.6406% |
| VoltageReversePeakCoefficient | 持续值预测 | 170287.2944% | 178395.8058% |
| VoltageMinAbsRaw | 持续值预测 | 5099.9214% | 5349.5711% |
| DischargePeriodSec | AR(1) 自回归 | 1.2870% | 1.3135% |

反峰系数和最小电压幅值序列含有接近 0 的真实值，MAPE 分母很小，因此百分比误差被大幅放大。这两列的 MAPE 不适合单独解释，网页同时保留 MAE、RMSE、最大误差和最后预测/真实值用于判断。

浏览器验证：

- 桌面视口 `1440 × 1000`：双页签切换、最佳预测图、最佳模型验证误差图和候选模型表正常显示。
- 移动视口 `390 × 844`：控件单列排列，摘要卡片换行，结果表保持可读且无文字重叠。
- `/api/evaluate`、`/api/validate`、`/api/compare-models` 均返回 HTTP 200。
- 浏览器控制台：0 个错误，0 个警告。
- 截图：`output/playwright/multi-model-page.png`、`output/playwright/multi-model-mobile.png`。
- 当前融合策略为等权平均，未实现自定义权重界面。

## 2026-07-20 严格特征深度学习独立测试

数据范围仅为 `ceshishuju_patent_features.csv` 的五个严格特征。模型输入最近 32 行，直接预测 20 行后的五参数值。数据严格按时间顺序划分：训练行 1-424、验证行 425-515、独立测试行 516-606；测试集未参与架构选择或早停。

训练环境：PyTorch 2.12.1、CUDA 13.0、NVIDIA GeForce RTX 5090。GRU、LSTM、TCN、Transformer 均采用残差预测，即以窗口末值为基准学习未来变化量，每种架构使用 3 个随机种子集成。

| 模型 | 类型 | 验证 NMAE | 独立测试 NMAE | 测试归一化精度 | 测试 sMAPE |
| --- | --- | ---: | ---: | ---: | ---: |
| 持续值 | 基线 | 17.62% | 16.20% | 83.80% | 29.41% |
| Ridge | 基线 | 20.07% | 17.21% | 82.79% | 28.62% |
| GRU | 深度学习，验证集选中 | 19.19% | 15.56% | 84.44% | 26.34% |
| LSTM | 深度学习 | 19.38% | 16.03% | 83.97% | 26.25% |
| TCN | 深度学习 | 20.35% | 18.02% | 81.98% | 30.44% |
| Transformer | 深度学习 | 19.23% | 16.05% | 83.95% | 26.85% |

GRU 是验证集选出的最佳深度架构，在独立测试集上也取得最低 NMAE。其测试 NMAE 比持续值基线降低 0.64 个百分点，属于小幅改进，不应解释为深度学习具有压倒性优势。

GRU 逐参数独立测试结果：

| 参数 | MAE | RMSE | NMAE | sMAPE |
| --- | ---: | ---: | ---: | ---: |
| VoltageMaxRaw | 38.414642 | 40.075069 | 5.00% | 0.13% |
| VoltageFirstZeroTimeUs | 0.001924 | 0.002383 | 6.87% | 0.66% |
| VoltageReversePeakCoefficient | 0.002765 | 0.003934 | 31.80% | 64.83% |
| VoltageMinAbsRaw | 83.361221 | 118.560593 | 32.69% | 64.46% |
| DischargePeriodSec | 0.088290 | 0.163404 | 1.42% | 1.64% |

结论：当前深度模型适合最大电压、首次过零时间和放电周期的 20 步预测；对反峰系数和最小电压幅值的离散跳变仍不够可靠。606 条记录仍属深度学习小样本，暂不应仅凭本次结果直接替代专利寿命预警逻辑。

网页与接口验证：

- 桌面视口 `1440 × 1000`：深度学习页、五特征误差曲线、逐参数结果和候选模型表完整显示。
- 移动视口 `390 × 844`：控制区和摘要区按单列排列，页面主体宽度与视口一致；宽表只在表格容器内横向滚动，无文字、控件重叠或全页横向溢出。
- 六个模型均保存五个严格特征的 91 个独立测试预测点；网页可切换模型和参数查看真实值实线与预测值虚线。
- 模型表按测试 NMAE 从低到高显示排名，当前第 1 名为 GRU（NMAE 15.56%，归一化精度 84.44%）。
- GRU 寿命轨迹包含最近 32 个实测点和未来 5000 个递推预测点；五个特征均无 NaN、无穷大或负值。
- 默认勾选最大电压和首次过零时间，按当前值及 5%/20% 阈值计算，5000 步内未到临界，因此剩余寿命显示 `>5000 步`、总寿命显示 `>5606 步`。
- 阈值联动测试：将最大电压下降阈值改为 0.1% 后，网页计算剩余 23 步、总寿命 629 步，并正确标记最大电压为限制寿命参数；恢复 5% 后结果恢复为 `>5000 步`。
- `/api/datasets`、`/api/deep-learning-report`、`/api/evaluate`、`/api/validate`、`/api/compare-models` 均返回 HTTP 200。
- 浏览器控制台：0 个错误，0 个警告。
- 截图：`output/playwright/strict-deep-learning-page.png`、`output/playwright/strict-deep-learning-mobile.png`。

## 2026-07-20 全部模型统一比较与建模步数搜索

本轮把专利模型、统计方法、基线和深度网络统一到同一时间顺序测试口径。固定预测第 20 步目标；训练区为第 1-424 行，验证区为第 425-515 行，独立测试区为第 516-606 行。每个模型只根据验证集从 `8、16、24、32、48、64、80、120` 步中选择自己的最佳建模窗口，然后在相同的 91 个独立测试目标行上比较。

| 排名 | 模型 | 类型 | 最佳建模步数 | 验证 NMAE | 测试 NMAE | 测试归一化精度 |
| ---: | --- | --- | ---: | ---: | ---: | ---: |
| 1 | GRU | 深度学习 | 32 | 19.19% | 15.56% | 84.44% |
| 2 | LSTM | 深度学习 | 8 | 19.25% | 15.62% | 84.38% |
| 3 | Ridge 线性基线 | 基线 | 8 | 19.12% | 16.11% | 83.89% |
| 4 | 持续值基线 | 基线 | 8 | 17.62% | 16.20% | 83.80% |
| 5 | Transformer | 深度学习 | 80 | 17.86% | 16.82% | 83.18% |
| 6 | AR(1) 自回归 | 统计模型 | 16 | 18.11% | 17.31% | 82.69% |
| 7 | TCN | 深度学习 | 8 | 20.07% | 17.65% | 82.35% |
| 8 | 阻尼 Holt 趋势 | 统计模型 | 120 | 18.31% | 17.83% | 82.17% |
| 9 | GM(1,1) 灰色模型 | 专利模型 | 80 | 17.85% | 19.72% | 80.28% |
| 10 | 线性趋势回归 | 统计模型 | 80 | 17.67% | 20.01% | 79.99% |

结论：全部模型中 GRU 的独立测试 NMAE 最低，表现最高；其建模步数由验证集确定为 32 步。专利 GM(1,1) 在候选窗口中以 80 步最好，但本次独立测试排名第 9。持续值在验证集最低，说明序列短期变化较弱；“独立测试最高”只用于报告最终表现，不用于反向修改窗口或训练参数。

训练报告包含 10 个模型 × 5 个参数 × 91 个测试点，每个点均保存真实值、预测值和归一化绝对误差。寿命预测继续使用最终测试表现最高的 GRU，并从第 606 行向未来递推 5000 步。

最终验证：

- `python3 -m unittest discover -s tests -v`：12 项全部通过。
- 报告完整性：10 个模型、50 组逐参数曲线、4550 对有限实测/预测值、25000 个有限寿命预测值。
- 浏览器自动遍历 10 个模型 × 5 个参数共 50 种组合；每种组合均检测到真实实线、预测虚线、91 个测试点、正确标题和悬停数值，失败 0 组。
- 预测图、寿命图、误差图和专利图均显示横纵十字辅助线；未来寿命区只显示预测值和阈值，不把最后实测点误标为未来真实值。
- 桌面视口 `1440 × 1000` 和移动视口 `390 × 844` 均完成截图检查；移动端全文宽度等于视口宽度，宽表只在表格容器内滚动，无页面级横向溢出、控件重叠或图例裁切。
- 浏览器控制台 0 条错误、警告或问题；动态表单均有 `id/name`。
- `/api/datasets`、`/api/deep-learning-report`、`/api/evaluate`、`/api/validate`、`/api/compare-models` 全部返回 HTTP 200。

## 2026-07-21 可重复浏览器回归

新增 `tests/browser/frontend.spec.js` 和 `playwright.config.js`，把此前人工执行的浏览器检查固化为 `npm run test:ui`。测试默认自动启动独立的 `127.0.0.1:8891` 服务，不影响正在运行的 `8790/8890`。

自动检查内容：

- 遍历 10 个模型 × 5 个严格特征共 50 种组合，检查标题、真实值实线和预测值虚线；报告中的每组曲线均为 91 个测试点。
- 检查“全部模型对比预测”位于“专利 GM(1,1)”左侧，两个页签都能正确切换和绘图。
- 检查预测图悬停提示包含目标行、真实值、模型预测值，并同时显示横纵辅助线。
- 在 `390 × 844` 视口检查页面级横向溢出、图表边界以及悬停框不越出图表区域。
- 监听浏览器控制台、页面异常、失败请求和 HTTP 4xx/5xx。

执行结果：Playwright 3 项全部通过，后端 12 项全部通过。测试过程中发现并修复了客户端刷新时服务端重复输出 `BrokenPipeError` 的问题；现在浏览器取消尚未写完的响应时会正常关闭该连接，不再产生伪内部错误日志。

## 2026-07-21 电流换算确认与量化感知精度优化

已确认 OWON CH1 采集时启用了电流换算。程序保留历史 `Voltage*Raw` 字段名用于接口兼容，但页面统一标注为“CH1 电流换算采样”，不再解释为电容器端真实电压。固定比例单位换算不会改变 NMAE 或模型排名，因此本轮保持原始采样量纲，避免在缺少完整探头标定时误标单位。

数据诊断发现：主幅值仅 4 个档位、首次过零时间 19 个档位、反向最小幅值仅 2 个档位；反向峰系数又几乎由反向幅值除以主幅值决定。新增“量化感知混合模型”：主幅值使用最近窗口上包络，首次过零时间使用中位数，反向幅值使用众数档位，反向峰系数由两种预测幅值重新计算，放电周期使用训练段有效周期中位数。

时间顺序划分仍为训练行 `1-424`、验证行 `425-515`、独立测试行 `516-606`，统一预测第 20 步。模型和建模窗口只由验证集选择；24、32、48 步验证 NMAE 同为 13.3263%，按预先规定的并列规则取中间窗口 32 步，未使用测试集打破并列。

| 排名 | 模型 | 建模步数 | 验证 NMAE | 测试 NMAE | 测试归一化精度 |
| ---: | --- | ---: | ---: | ---: | ---: |
| 1 | 量化感知混合模型 | 32 | 13.33% | 10.68% | 89.32% |
| 2 | GRU | 32 | 19.19% | 15.56% | 84.44% |
| 3 | LSTM | 8 | 19.25% | 15.62% | 84.38% |
| 4 | Ridge 线性基线 | 8 | 19.12% | 16.11% | 83.89% |
| 5 | 持续值基线 | 8 | 17.62% | 16.20% | 83.80% |
| 6 | Transformer | 80 | 17.86% | 16.82% | 83.18% |
| 7 | AR(1) 自回归 | 16 | 18.11% | 17.31% | 82.69% |
| 8 | TCN | 8 | 20.07% | 17.65% | 82.35% |
| 9 | 阻尼 Holt 趋势 | 120 | 18.31% | 17.83% | 82.17% |
| 10 | GM(1,1) 灰色模型 | 80 | 17.85% | 19.72% | 80.28% |
| 11 | 线性趋势回归 | 80 | 17.67% | 20.01% | 79.99% |

新模型相对原测试第一名 GRU 的精度提高 4.88 个百分点，相对持续值基线提高 5.52 个百分点。逐参数测试 NMAE 分别为：主幅值 0.00%、首次过零时间 6.57%、反向峰系数 22.49%、反向最小幅值 23.08%、放电周期 1.27%。后两个参数仍受两档 ADC 量化限制，不能把 89.32% 解读为模拟波形已恢复到该精度。

寿命阈值现固定使用原始数据第 1 次放电值；放电周期因第 1 次尚无间隔，使用第 1 次至第 2 次之间的首个有效间隔。参数权重同时考虑独立测试误差和量化分辨率。有限寿命还要求至少两个窗口同向退化，并且阈值交叉落在经验证时距内；任一关键参数最先失效即限制系统寿命。

新增真实失效标签：参考电容在现有 606 次记录后又运行 1500 次失效，即总寿命 2106 次。使用 Theil-Sen 稳健趋势外推至该失效点并相对第 1 次放电反标后，最大幅值阈值为上升 6.98%（31768.7808），首次过零时间阈值为下降 23.32%（0.2407752 μs）。其余三个参数缺少稳定单调失效趋势，未使用该标签强行反标。该真实寿命参与阈值标定，因此不得同时作为独立寿命预测精度证据。

新增 1-500 步多时距独立回测：20 步精度 89.32%，120 步精度 87.46%，160 步降为 83.99%。以 85% 为最低精度，最长验证时距为 120 步。当前默认五参数健康度为 95.9%、信息可信度 52.4%，没有一致失效趋势，因此显示剩余寿命 `>120 步（约 10.8 分钟）`、总寿命 `>726 步`；5000 步未到阈值明确标为未验证探索性外推。

最终验证：后端 12 项单元测试和 Playwright 4 项全部通过；浏览器遍历 55 组模型/参数曲线。另行核验寿命图的实测、验证预测、探索外推和阈值四色像素，桌面与 390 像素移动端无溢出或图例截断，寿命图悬停横纵辅助线及具体数值正常，控制台和接口均无错误。
